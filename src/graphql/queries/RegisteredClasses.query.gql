query RegisteredClasses($registrationId: ID!) {
	registration(id: $registrationId) {
		registeredClasses {
			category
			classNumber
			discipline
			id
			level
			numberOfSelections
			price
			subdiscipline
			schoolCommunityId
			selections {
				composer
				duration
				id
				largerWork
				movement
				title
			}
		}
	}
}
