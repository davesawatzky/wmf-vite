mutation PerformerUpdate($performerId: ID!, $performer: PerformerInput!) {
	performerUpdate(performerID: $performerId, performer: $performer) {
		performer {
			id
		}
		userErrors {
			message
		}
	}
}
