mutation CommunityCreate($registrationId: ID!, $community: CommunityInput!) {
	communityCreate(registrationID: $registrationId, community: $community) {
		community {
			id
		}
		userErrors {
			message
		}
	}
}
