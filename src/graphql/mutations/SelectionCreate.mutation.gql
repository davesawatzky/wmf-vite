mutation SelectionCreate($registeredClassId: ID!, $selection: SelectionInput!) {
	selectionCreate(
		registeredClassID: $registeredClassId
		selection: $selection
	) {
		selection {
			id
		}
		userErrors {
			message
		}
	}
}
