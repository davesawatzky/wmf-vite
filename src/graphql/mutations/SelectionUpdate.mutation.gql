mutation SelectionUpdate($selectionId: ID!, $selection: SelectionInput!) {
	selectionUpdate(selectionID: $selectionId, selection: $selection) {
		selection {
			id
		}
		userErrors {
			message
		}
	}
}
